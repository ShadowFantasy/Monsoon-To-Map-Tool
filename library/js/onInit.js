"use strict";
try{
    var namespace = "net.shadow.fantasy.lib.monsoon.sheetapi";
    MapTool.chat.broadcast(`created validateMastersheet`);
} catch (e) {
    MapTool.chat.broadcast(`${e}, ${e.stack}`);
}